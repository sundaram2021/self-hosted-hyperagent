{
  "name": "@hyperagent/server",
  "version": "0.1.0",
  "private": true,
  "description": "Agent runtime server — owns the agent loop, SSE streaming, and MCP connections.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.build.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@fastify/cors": "^11.0.0",
    "@hyperagent/ai": "workspace:*",
    "@hyperagent/db": "workspace:*",
    "@hyperagent/shared": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "ai": "^6.0.0",
    "drizzle-orm": "^0.44.0",
    "fastify": "^5.3.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.3.0",
    "@types/node": "^22.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.0"
  }
}
